machine:
  java:
    version: oraclejdk7
  services:
    - mysql

database:
  override:
    - mysql -e " SET PASSWORD FOR 'root'@'localhost' = PASSWORD('123456') " -uroot

test:
  override:
    - mvn clean
    - mvn compile install -Dmaven.test.skip=true -P test
    - mvn test -B -X

# 郁闷啊,x 测试 api